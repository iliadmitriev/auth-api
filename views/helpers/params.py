default_parameters = [
    {
        "in": "header",
        "name": "Authorization",
        "type": "string",
        "format": "Bearer",
        "required": "true",
        "default": "Bearer asdf.qwer.zxcv",
        "description": "Authorization HTTP header with JWT access token, like: **Bearer asdf.qwer.zxcv**",
    }
]
